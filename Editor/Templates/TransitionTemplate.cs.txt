using UnityEngine;
using U.Universal.Scenes;

public static partial class Uscenes  // Dir: Assets/Scripts/Uscenes/*Transitions.cs
{
    public static partial class Transitions
    {
        public static ISceneTransition[] #SCRIPTNAME# => new ISceneTransition[]
        {

            /*
            new SceneTransition
            {
                currentScenePattern = "Intro",
                nextScenePattern = "Game",

                //setUp = () => Debug.Log("Uscenes: setUp"),
                //setUpProgress = (p) => Debug.Log("Uscenes: setUpProgress = " + p),
                //setUpReady = () => Debug.Log("Uscenes: setUpReady"),
                //loadProgres = (p) => Debug.Log("Uscenes: loadProgres = " + p),
                //tearDown = () => Debug.Log("Uscenes: tearDown"),
                //tearDownProgress = (p) => Debug.Log("Uscenes: tearDownProgress = " + p),
                //tearDownReady = () => Debug.Log("Uscenes: tearDownReady"),
            },
            */

        };
    }
}