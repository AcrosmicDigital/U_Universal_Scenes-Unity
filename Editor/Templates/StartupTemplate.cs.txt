using UnityEngine;
using U.Universal.Scenes;

public static partial class Uscenes
{

    [RuntimeInitializeOnLoadMethod]
    public static void Startup()
    {

        if (true)  // Disable with false
        {

            // Apply selectors
            //SceneMonitor.Apply(Uscenes.Selectors.ProdSelectors);

            // Apply transitions
            //SceneMonitor.Apply(Uscenes.Transitions.ProdTransitions);

            // Enable
            //SceneMonitor.Enable();

        }
    }

}