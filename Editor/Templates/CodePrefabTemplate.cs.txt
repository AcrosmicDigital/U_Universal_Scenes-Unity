using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public static partial class CodePrefabs
{
    public static partial class Scenes
    {

        public static GameObject #SCRIPTNAME#(float alpha)
        {
            var go = new GameObject("#SCRIPTNAME#");

            //go.transform.localScale = new Vector3(8, 12, 1);
            //go.transform.position = Vector3.zero;

            //var sr = go.AddComponent<SpriteRenderer>();
            //sr.sprite = Resources.Load<Sprite>("Square_x128");
            //sr.color = new Color(0, 0, 0, alpha);
            //sr.sortingLayerID = 0;
            //sr.sortingOrder = 100;

            return go;
        }

    }
}
